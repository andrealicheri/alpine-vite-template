/* main.ts should be used only as a gateway file to import all of your other TypeScript code.
Refrain from inserting statements other than imports in this file if not for testing or debugging purposes.
Also note that main.ts is imported with a defer attribute in the index file.*/

import "@picocss/pico/css/pico.min.css"
import "../src/ts/alpine.ts"
import "htmx.org"

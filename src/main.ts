/* main.ts should be used only as a gateway file to import all of your other TypeScript code.
Refrain from inserting statements other than imports in this file if not for testing or debugging 
purposes. */

import "../src/ts/picocss.ts"
import "../src/ts/alpine.ts"
import htmx from "htmx/htmx.js"
